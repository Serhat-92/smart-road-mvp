# Apps

This folder contains user-facing entrypoints built on top of shared services.

- `radar-cli/`: local field-unit launcher and CLI passthrough
- `command-center/`: FastAPI HQ dashboard
- `operator-dashboard/`: React + Vite operator console with backend-ready mock data layer
