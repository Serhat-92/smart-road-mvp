# Integration Tests

Integration and smoke checks that exercise service-to-app flows belong here.

- `test_gateway_detection_events.py`: verifies gateway event ingestion, shared detection schema validation, and in-memory persistence.
