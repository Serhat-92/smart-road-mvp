# Shared Python

Reusable Python helpers live here.

- `monorepo.py`: bootstrap local import paths for script entrypoints
- `radar_config.py`: persisted launcher configuration helpers

`monorepo.py` also adds `shared/event-contracts/python` so services can import the shared contract models.
